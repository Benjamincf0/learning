[project]
name = "test-wraps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=9.1.1",
]

[dependency-groups]
dev = [
    "basedpyright>=1.39.8",
]
